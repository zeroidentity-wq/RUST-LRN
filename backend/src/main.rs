// Backend projects — Faza 1 (fara async, fara threaduri)
// Run individual projects with: cargo run --bin <name> -p backend
fn main() {
    println!("Backend projects — run with: cargo run --bin <project_name> -p backend");
}
