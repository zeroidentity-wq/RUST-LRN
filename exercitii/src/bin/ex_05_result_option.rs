// Exercitii 05 — Option si Result
// Rulare: cargo run --bin ex_05_result_option

#[derive(Debug)]
enum Item {
    Sabie(i32),
    Armura(i32),
    Potion(i32),
    QuestItem(String),
}

#[derive(Debug)]
struct Erou {
    nume: String,
    hp: i32,
    mana: i32,
    inventar: Vec<Item>,
}

// Scrie codul aici

fn main() {

}
